/// <reference types="cypress" />

describe('feature a', () => {
  it('works', () => {})

  it('works 2', () => {})

  it('fails', () => {
    expect(false).to.equal(true)
  })

  it('fails again', () => {
    expect(false).to.equal(true)
  })

  it('works 3', () => {})
})
